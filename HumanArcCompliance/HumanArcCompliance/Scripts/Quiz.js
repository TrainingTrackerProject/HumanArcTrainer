/*
 * AngularJS code for the Quiz application, called on Quiz.cshtml.
 */

var quizApp = angular.module('QuizApp', ['ngRoute']);

quizApp.controller('QuizCtrl', function ($scope, $http) {
    //var allQuestions to be an array with all questions in the quiz. Called on ng-repeat on view.
});